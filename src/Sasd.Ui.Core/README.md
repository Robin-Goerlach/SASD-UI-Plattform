# Sasd.Ui.Core

Platform-neutral contracts and visual semantics. This project must not reference WinForms or third-party UI libraries.

Initial scaffold types:

- `UiOperationResult` and `UiOperationResult<T>`;
- `ThemeMode`;
- semantic theme-token records.

Further public APIs are added only through reviewed requirements and ADRs.
