# Sasd.Ui.WinForms

The designer-friendly WinForms foundation. It references `Sasd.Ui.Core` and `System.Windows.Forms`, but no optional visual or specialist suite.

The R0 scaffold includes only a small base form, base user control and UI-thread dispatcher. Search, empty/busy states and other R1 composites are introduced through the approved backlog.
